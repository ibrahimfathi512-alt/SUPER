<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>دخول المشرفين | Tanta Car</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            margin: 0; padding: 0;
            display: flex; justify-content: center; align-items: center;
            min-height: 100vh;
            /* استخدام رابط مباشر للخلفية لضمان ظهورها فوراً */
            background: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)), 
                        url('https://images.unsplash.com/photo-1626125336093-026859739a82?q=80&w=1600&auto=format&fit=crop');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .login-card {
            background: rgba(45, 45, 45, 0.9);
            width: 380px;
            padding: 40px 30px;
            border-radius: 35px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.6);
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(8px);
        }

        .top-badge {
            background: #ff7e20;
            color: #fff;
            padding: 6px 18px;
            border-radius: 50px;
            font-size: 12px;
            font-weight: bold;
            display: inline-block;
            margin-bottom: 25px;
        }

        .logo-wrapper { font-size: 60px; color: #ff7e20; margin-bottom: 15px; }
        h1 { color: #ffffff; font-size: 24px; margin: 10px 0; }
        .slogan { color: #ffb400; font-size: 13px; margin-bottom: 30px; }

        .input-group { position: relative; margin-bottom: 20px; }
        .input-group i {
            position: absolute; right: 15px; top: 50%;
            transform: translateY(-50%); color: #ff7e20;
        }

        select, input {
            width: 100%;
            padding: 15px 45px 15px 15px;
            background: #1e1e1e;
            border: 1px solid #444;
            border-radius: 12px;
            color: white;
            font-size: 15px;
            outline: none;
            appearance: none;
        }

        .submit-btn {
            width: 100%;
            padding: 15px;
            background: linear-gradient(45deg, #ff8c00, #ff4500);
            border: none;
            border-radius: 12px;
            color: white;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            box-shadow: 0 10px 20px rgba(255, 69, 0, 0.3);
            transition: 0.3s;
        }

        .submit-btn:hover { transform: translateY(-3px); filter: brightness(1.1); }

        .footer-text { margin-top: 35px; color: #ccc; font-size: 11px; }
        .footer-text span { display: block; color: #fff; font-weight: bold; margin: 5px 0; }
    </style>
</head>
<body>
    <div class="login-card">
        <div class="top-badge">مكتب طنطا كار | TANTA CAR</div>
        <div class="logo-wrapper"><i class="fas fa-motorcycle"></i></div>
        <h1>دخول المشرفين</h1>
        <p class="slogan">صلّ على النبي ﷺ</p>

        <form action="/login" method="POST">
            <div class="input-group">
                <i class="fas fa-map-marker-alt"></i>
                <select name="zone" required>
                    <option value="" disabled selected>اختر الزون الخاص بك</option>
                    <% if (typeof zones !== 'undefined') { %>
                        <% zones.forEach(function(zone) { %>
                            <option value="<%= zone %>"><%= zone %></option>
                        <% }); %>
                    <% } %>
                </select>
            </div>
            <div class="input-group">
                <i class="fas fa-lock"></i>
                <input type="password" name="password" placeholder="كلمة المرور" required>
            </div>
            <button type="submit" class="submit-btn">دخول النظام</button>
        </form>

        <div class="footer-text">
            تمت البرمجة بواسطة المشرف
            <span>Ibrahim Fathi</span>
            <i class="fas fa-phone-alt"></i> 01550536446
        </div>
    </div>
</body>
</html>